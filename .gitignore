node_modules
dist
.next
.turbo
.env
.env.local
.DS_Store
*.log
coverage
.vscode
.idea
*.db
*.db-journal
*.sqlite
*.sqlite-journal
prisma/migrations/dev/

# Cloudflare Worker local state (miniflare)
.wrangler/
.dev.vars

# Eval datasets — large, often license-restricted, regenerable
apps/web/eval/data/

# Build / cache
.tsbuildinfo
*.tsbuildinfo

# local apply review artifact
hap-application.json
# local living-profile artifact
hap-profile.json
# local apply ledger (rate-cap/dedupe)
hap-applied.json
# test fixture
_cc_fixture/
