{
  "name": "@hap/proxy",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "build": "echo 'no build — Cloudflare Worker deploys via wrangler'",
    "dev": "wrangler dev --local --port 8787",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "kv:create": "wrangler kv:namespace create QUOTA_KV"
  },
  "dependencies": {
    "hono": "^4.6.13"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241218.0",
    "typescript": "^5.6.3",
    "wrangler": "^3.91.0"
  }
}
